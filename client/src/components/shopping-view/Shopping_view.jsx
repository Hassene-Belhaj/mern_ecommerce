import React from "react";
import { Outlet } from "react-router-dom";

const Shopping_view = () => {
  return (
    <div>
      ShoppingView <Outlet />
    </div>
  );
};

export default Shopping_view;
